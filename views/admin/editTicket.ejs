<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Ticket</title>
</head>
<body>
    <h1>Edit Ticket</h1>
    <form action="/admin/tickets/<%= ticket.id %>/edit" method="POST">
        <label for="name">Name:</label>
        <input type="text" id="name" name="name" value="<%= ticket.name %>" required><br><br>
        <label for="date">Date:</label>
        <input type="date" id="date" name="date" value="<%= ticket.date.toISOString().split('T')[0] %>" required><br><br>
        <label for="place">Place:</label>
        <input type="text" id="place" name="place" value="<%= ticket.place %>" required><br><br>
        <label for="typeSeat">Type of Seat:</label>
        <select id="typeSeat" name="typeSeat" required>
            <option value="VIP" <% if (ticket.typeSeat === 'VIP') { %> selected <% } %>>VIP</option>
            <option value="Regular" <% if (ticket.typeSeat === 'Regular') { %> selected <% } %>>Regular</option>
            <option value="Economy" <% if (ticket.typeSeat === 'Economy') { %> selected <% } %>>Economy</option>
        </select><br><br>
        <label for="stock">Stock:</label>
        <input type="number" id="stock" name="stock" value="<%= ticket.stock %>" required><br><br>
        <label for="price">Price:</label>
        <input type="number" id="price" name="price" value="<%= ticket.price %>" required><br><br>
        <input type="submit" value="Update Ticket">
    </form>
</body>
</html>
