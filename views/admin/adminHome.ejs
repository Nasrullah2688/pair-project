<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Home</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: white;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }
        h1 {
            text-align: center;
            margin-bottom: 20px;
        }
        .add-ticket {
            display: block;
            width: fit-content;
            margin: 0 auto 20px;
            padding: 10px 20px;
            text-align: center;
            background-color: #78bd00;
            color: white;
            text-decoration: none;
            border-radius: 4px;
            transition: background-color 0.3s;
        }
        .add-ticket:hover {
            background-color: #456d00;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        th {
            background-color: #878787;
            color: white;
        }
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        tr:hover {
            background-color: #f1f1f1;
        }
        .action-buttons {
            display: flex;
            gap: 10px;
        }
        .action-buttons a,
        .action-buttons button {
            padding: 5px 10px;
            text-align: center;
            text-decoration: none;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s, color 0.3s;
        }
        .action-buttons a {
            background-color: #78bd00;
            color: white;
        }
        .action-buttons a:hover {
            background-color: #456d00;
        }
        .action-buttons button {
            background-color: #FF4B4B;
            color: white;
        }
        .action-buttons button:hover {
            background-color: #CC0000;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Manajemen Tiket oleh Admin</h1>
        <a href="/admin/tickets/add" class="add-ticket">Add Ticket</a>
        <a href="/logout" class="btn-logout">Logout</a>
        <h5 style="color: red;">
            <% if (name) { %>
            <%= name %> has been removed
            <% } %>
        </h5>
        <table>
            <tr>
                <th>Name</th>
                <th>Date</th>
                <th>Place</th>
                <th>Action</th>
            </tr>
            <% data.forEach(ticket => { %>
                <tr>
                    <td><%= ticket.name %></td>
                    <td><%= ticket.date.toLocaleDateString() %></td>
                    <td><%= ticket.place %></td>
                    <td>
                        <div class="action-buttons">
                            <a href="/admin/tickets/<%= ticket.id %>/edit">Edit</a>
                            <form action="/admin/tickets/<%= ticket.id %>/delete" method="POST" onsubmit="return confirm('Are you sure you want to delete this ticket?')">
                                <button type="submit">Delete</button>
                            </form>
                        </div>
                    </td>
                </tr>
            <% }) %>
        </table>
    </div>
</body>
</html>
