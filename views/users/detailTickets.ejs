<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detail Tiket</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 845px;
            margin: 40px auto;
            background-color: white;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            overflow: hidden;
        }
        h1 {
            background-color: #727375;
            color: white;
            padding: 20px;
            margin: 0;
            text-align: center;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            padding: 15px;
            text-align: left;
        }
        th {
            background-color: #076B4E;
            color: white;
        }
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
        .action-form {
            text-align: center;
        }
        .action-form input[type="number"] {
            width: 60px;
            padding: 5px;
            margin-right: 10px;
            border-radius: 4px;
            border: 1px solid #ccc;
        }
        .action-form input[type="submit"] {
            padding: 8px 12px;
            border: none;
            background-color: #d12626;
            color: white;
            border-radius: 4px;
            cursor: pointer;
        }
        .action-form input[type="submit"]:hover {
            background-color: #8b0000;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Detail Tiket</h1>
        <table border="1">
            <tr>
                <th>Nama</th>
                <th>Tiket Tersedia</th>
                <th>Tempat</th>
                <th>Harga</th>
                <th>Aksi</th>
            </tr>
            <tr>
                <td><%= data.name %></td>
                <td><%= data.stock %></td>
                <td><%= data.place %></td>
                <td><%= formatRupiah(data.price) %></td>
                <td class="action-form">
                    <form action="/tickets/<%= data.id %>/take" method="POST">
                        <input type="number" name="stock" min="1" max="<%= data.stock %>" required>
                        <input type="submit" value="Ambil">
                    </form>
                </td>
            </tr>
        </table>
    </div>
</body>
</html>